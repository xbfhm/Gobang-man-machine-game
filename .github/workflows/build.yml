name: Build APK (Android 16)

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install system dependencies
        run: |
          sudo apt update
          sudo apt install -y git zip unzip python3-pip build-essential autoconf \
            libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev \
            libtinfo5 libtinfo6 cmake libffi-dev libssl-dev automake \
            autopoint gettext ccache

      - name: Cache buildozer global directory
        uses: actions/cache@v4
        with:
          path: ~/.buildozer/cache
          key: buildozer-global-${{ runner.os }}-${{ hashFiles('buildozer.spec') }}
          restore-keys: |
            buildozer-global-${{ runner.os }}-

      - name: Install buildozer and cython
        run: |
          pip install --upgrade pip setuptools wheel
          pip install "buildozer>=1.5.0" "cython>=3.0.0,<3.1"

      - name: Clean previous buildozer artifacts
        run: rm -rf .buildozer/

      - name: Build debug APK (targetSdk 36 / NDK 28c / arm64-v8a)
        run: |
          set -o pipefail
          export APP_ALLOW_MISSING_DEPS=true
          # 使用 python-for-android develop 分支（buildozer.spec 中 p4a.branch = develop）
          buildozer -v android debug 2>&1 | tee build_log.txt

      - name: Verify 16KB page size alignment
        run: |
          APK=$(ls bin/*.apk 2>/dev/null | head -1)
          if [ -z "$APK" ]; then echo "::error::No APK produced"; exit 1; fi
          echo "APK: $APK"
          mkdir -p /tmp/apk
          unzip -q "$APK" -d /tmp/apk
          NDK=$(ls -d ~/.buildozer/android/platform/android-ndk-* 2>/dev/null | head -1)
          echo "NDK: $NDK"
          if [ -n "$NDK" ]; then
            READELF="$NDK/toolchains/llvm/prebuilt/linux-x86_64/bin/llvm-readelf"
            for so in /tmp/apk/lib/arm64-v8a/*.so; do
              echo "== $(basename $so) =="
              "$READELF" -l "$so" 2>/dev/null | awk '/LOAD/ {print $NF}' | sort -u
            done
          fi
          echo "16KB alignment check done (align 2**14 = 16384 means OK)"

      - name: Locate and copy APKs
        run: |
          mkdir -p artifact
          cp bin/*.apk artifact/ 2>/dev/null || true
          ls -la artifact/

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: gomoku16-apk
          path: artifact/*.apk

      - name: Upload Build Log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build_log.txt
